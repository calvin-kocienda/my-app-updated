<%= form_with model: fooditems do |form| %>
  <div>
    <%= form.label :item %><br>
    <%= form.text_field :item %>

    <% end %>
  </div>

  <div>
    <%= form.label :price2 %><br>
    <%= form.text_area :price2 %><br>

    <% end %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<!-- 
	 <td><%= check_box_tag "isshowing", false %>
	 <%= label_tag "" %></td>
	 <td><%= form_with(url: '/modify', method: :get, local: true) do |form| %>
		<%= form.select :category, ["Old", "New", "Best Selling"] %>
	 <% end %></td>
	 <td><%= button_to "Delete Item", '/modify', method: :delete, data: {confirm: "Are you sure?"} %></td> -->